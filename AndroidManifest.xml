<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kuliah.pm2aplikasiidentitas"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.kuliah.pm2aplikasiidentitas.SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="DeleteActivity"></activity>
        <activity android:name="InsertActivity"></activity>
        <activity android:name="UpdateActivity"></activity>
        <activity android:name="ReportActivity"></activity>
        <activity android:name="FormUpdateActivity"></activity>
        <activity android:name="MainActivity"></activity>
    </application>

</manifest>
